<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CodeFont" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="RedText" parent="@android:style/TextAppearance">
        <item name="android:textColor">#FF0000</item>
    </style>

    <style name="RedButton" parent="@style/Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">#FF0000</item>
    </style>

    <style name="RedText.NewText">
        <item name="android:typeface">monospace</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="CustomTheme" parent="AppTheme">
        <item name="android:textViewStyle">@style/RedText.NewText</item>
        <item name="android:buttonStyle">@style/RedButton</item>
    </style>
</resources>
